platform :ios, '9.0'

source 'https://github.com/CocoaPods/Specs.git'

use_frameworks!

target “Spry” do

end

target 'SpryTests' do
        pod 'Spry+Nimble', :path => ‘./‘
        pod 'Quick'
        pod 'Nimble'
end
