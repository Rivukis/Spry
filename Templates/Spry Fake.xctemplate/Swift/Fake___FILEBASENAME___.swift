import Foundation

import Spry

@testable import ___PROJECTNAME___

class Fake___VARIABLE_spryClass:identifier___: ___VARIABLE_spryClass:identifier___, Spryable {

  enum Function: String, StringRepresentable {
    case <#someFunction#> = <#"someFunction()"#>
  }

  //MARK: - overrides -

  // Override methods here. Within each, use recordCall() to spy, stubbedValue() to stub, or spryify()
  // to do both. within them, and create a corresponding stringified case in the Function enum above.

}
